# Operator-uploaded media. The server writes these at runtime (real
# submissions, e2e tests, manual QA) so they shouldn't be tracked in
# version control. The folder itself is created on demand by multer.
uploads/*
!uploads/.gitkeep
